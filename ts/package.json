{
  "name": "solar-line",
  "version": "0.1.0",
  "private": true,
  "description": "SOLAR LINE 考察 — scripts and reports",
  "type": "module",
  "scripts": {
    "test": "node --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "node --experimental-strip-types src/build.ts --data-dir ../reports --out-dir ../dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
